--- a/realmwright-v7.html
+++ b/realmwright-v7.html
@@ -17285,6 +17285,52 @@
   _bound:false,
   _IDB_KEY:'rw_recent_generations',
   _MAX_RECENT:5,
+  _pendingDemoCanon:null,
+
+  // Compact, bounded context for the hosted five-use demo. This is intentionally
+  // separate from the paid Copilot context builder: the paid interface remains
+  // locked, while the demo gets enough of the active realm to propose useful,
+  // reviewable changes without exceeding the Worker's 16 KB prompt ceiling.
+  _demoRealmContext(){
+    const n=(typeof State!=='undefined'&&State.get)?State.get():null;
+    if(!n)return{nation:null,text:'No active realm is open. Give session-prep prose only; do not emit a [CANON] block.'};
+    const clip=(v,max=220)=>String(v??'').replace(/\s+/g,' ').trim().slice(0,max);
+    const lines=[
+      `Realm: ${clip(n.name,100)}`,
+      `Era: ${clip(n.era,80)} | Government: ${clip(n.government,80)} | Current year: ${clip(n.currentYear,20)}`,
+      `Leader: ${clip(n.leader,100)}`,
+    ];
+    const stats=Object.entries(n.stats||{}).slice(0,12).map(([key,v])=>`${clip(v?.displayName||key,50)} ${Number(v?.value??0)}`);
+    if(stats.length)lines.push(`Stats: ${stats.join('; ')}`);
+    const factions=(n.factions||[]).slice(0,3).map(f=>`${clip(f.name,70)} (${clip(f.position||f.type,40)})`);
+    if(factions.length)lines.push(`Factions: ${factions.join('; ')}`);
+    const chars=(n.characters||[]).slice(0,8).map(c=>`${clip(c.name,70)} — ${clip(c.role,70)}`);
+    if(chars.length)lines.push(`NPCs: ${chars.join('; ')}`);
+    const locs=(n.locations||[]).slice(0,6).map(l=>`${clip(l.name,70)} (${clip(l.type,50)})`);
+    if(locs.length)lines.push(`Locations: ${locs.join('; ')}`);
+    const recent=(n.chronicle||[]).slice().sort((a,b)=>(b.year||0)-(a.year||0)).slice(0,5).map(e=>`${clip(e.name,80)} — ${clip(e.description,160)}`);
+    if(recent.length)lines.push(`Recent chronicle: ${recent.join('; ')}`);
+    const hooks=(n.hooks||[]).filter(h=>h.status!=='resolved'&&!h.resolved).slice(0,4).map(h=>`${clip(h.title,80)} — ${clip(h.description,140)}`);
+    if(hooks.length)lines.push(`Open hooks: ${hooks.join('; ')}`);
+    return{nation:n,text:lines.join('\n').slice(0,6000)};
+  },
+
+  _demoSystemPrompt(style,hasRealm){
+    const canon=hasRealm?`\nAfter the prose, append a [CANON]...[/CANON] block containing 1-3 OPTIONAL changes the GM may review. Use only these exact line shapes:
+EVENT: Name | Political|Military|Economic|Cultural|Social|Diplomatic|Natural | weight -30..30 | public|private|forecast | description
+FACTION: Name | Government|Opposition|Religious|Economic|Military | holding|gaining|losing|fragmenting | description
+CHARACTER: Name | role | description
+LOCATION: Name | type | description
+HOOK: Title | description
+SECRET: Title | content
+STAT: Existing stat name | +N or -N
+Never state that a change has already been applied. Never include more than 3 lines.`:'\nThere is no active realm, so do not emit [CANON]. Mention briefly that opening or creating a realm enables reviewable changes.';
+    return`You are RealmWright's hosted demo guide. Give a vivid, concrete session-prep answer of about 180-240 words: an opening situation, an immediate pressure or hook, and one twist. Style: ${style}. The paid Copilot interface is locked, but this demo answer may offer reviewable world changes through the existing approval flow.${canon}\nDo not include sales copy inside the answer.`;
+  },
+
+  _stripCanonBlock(text){
+    return String(text||'').replace(/\[canon\][\s\S]*?\[\/canon\]/ig,'').trim();
+  },
 
   open(){
     const el=document.getElementById('modal-front-door');
@@ -17430,10 +17476,11 @@
     }
     if(btn){btn.disabled=true;btn.textContent='Generating…';}
     if(outBox){outBox.style.display='block';outBox.innerHTML='<div class="fd-demo__loading">The realm is taking shape…</div>';}
-    const sys=`You are RealmWright's demo Copilot. The visitor is exploring whether to buy. Give them a vivid 200-word taste of what a real session prep looks like — opening scene + one immediate hook + one twist. Style: ${style}. End with a single line: "(This is a free preview. Activate to unlock Tonight Mode, Solo Mode, and the complete Copilot — powered by a free NVIDIA model or your own AI key.)"`;
+    const demoCtx=this._demoRealmContext();
+    const sys=this._demoSystemPrompt(style,!!demoCtx.nation);
     const result=await Demo.proxyRequest([
       {role:'system',content:sys},
-      {role:'user',content:prompt},
+      {role:'user',content:`# Active realm context\n${demoCtx.text}\n\n# GM request\n${prompt}`},
     ],'anthropic/claude-haiku-latest');
     if(btn){btn.disabled=false;btn.textContent='Prep this session';}
     if(result.error){
@@ -17464,15 +17511,30 @@
     await DemoCounter.consume();
     await this._saveRecent({prompt,style});
     const content=result.content||'(no content returned)';
-    const safe=(typeof Markdown!=='undefined'&&Markdown.render)?Markdown.render(content):Utils.escHtml(content).replace(/\n/g,'<br>');
+    const parsed=(typeof Parse!=='undefined')?Parse.canonBlock(content,{detail:true}):{candidates:[],failures:[]};
+    const candidates=demoCtx.nation&&parsed&&Array.isArray(parsed.candidates)?parsed.candidates.slice(0,3):[];
+    const failures=demoCtx.nation&&parsed&&Array.isArray(parsed.failures)?parsed.failures.slice(0,8):[];
+    this._pendingDemoCanon=candidates.length?{nationId:demoCtx.nation.id,candidates,failures}:null;
+    const visible=this._stripCanonBlock(content)||(demoCtx.nation?'The demo prepared reviewable changes for your realm.':'No preview text was returned.');
+    const safe=(typeof Markdown!=='undefined'&&Markdown.render)?Markdown.render(visible):Utils.escHtml(visible).replace(/\n/g,'<br>');
     const remaining=typeof result.remaining_today==='number'?result.remaining_today:null;
     if(outBox){
       outBox.innerHTML=`
         <div class="fd-demo__result">${safe}</div>
         <div class="fd-demo__footer">
-          ${remaining!==null?`<span class="fd-demo__quota">${remaining} free preview${remaining===1?'':'s'} left in your one-time trial</span>`:''}
-          <button class="btn btn--primary btn--sm" type="button" data-fd-activate="true">Activate for full Tonight Mode</button>
+          ${remaining!==null?`<span class="fd-demo__quota">${remaining} free use${remaining===1?'':'s'} left in your one-time trial</span>`:''}
+          ${candidates.length?`<button class="btn btn--secondary btn--sm" type="button" data-fd-review="true">Review ${candidates.length} proposed world change${candidates.length===1?'':'s'}</button>`:''}
+          ${!demoCtx.nation?`<button class="btn btn--secondary btn--sm" type="button" data-fd-create="true">Create a realm for reviewable changes</button>`:''}
+          <button class="btn btn--primary btn--sm" type="button" data-fd-activate="true">Activate for full Copilot</button>
         </div>`;
+      const rb=outBox.querySelector('[data-fd-review]');
+      if(rb)rb.addEventListener('click',()=>{
+        const pending=this._pendingDemoCanon;if(!pending)return;
+        this.close();
+        setTimeout(()=>Modals.openCanonReview(pending.candidates,pending.nationId,pending.failures||[]),260);
+      });
+      const cb=outBox.querySelector('[data-fd-create]');
+      if(cb)cb.addEventListener('click',()=>{this.close();if(typeof Modals!=='undefined')Modals.open('new-nation');});
       const ab=outBox.querySelector('[data-fd-activate]');
       if(ab)ab.addEventListener('click',()=>LicenseGateUI.openModal());
     }
