--- a/realmwright-v7.html
+++ b/realmwright-v7.html
@@ -7507,6 +7507,11 @@
       }
     }
     this._write(survivors);
+    // A pending deactivation keeps the device token so retries can revoke the
+    // exact Worker binding. Clear it only after every queued operation succeeds.
+    if(survivors.length===0){
+      try{await IDB.set(IDB_KEY_DEVICE_TOKEN,null);}catch(e){/* stale local token is non-authoritative */}
+    }
   },
 };
 
@@ -7633,10 +7638,13 @@
     const ls=current.platform==='lemonsqueezy'&&current.instanceId
       ?{license_key:current.key,instance_id:current.instanceId}
       :null;
-    // Commit the local lock first and atomically. If storage rejects, keep the
-    // current license live and do not deactivate the server behind the user's back.
+    // Commit the local lock first. For Lemon Squeezy, retain the device token
+    // until the Worker confirms deactivation so X-Device-Token can revoke the
+    // exact server-side binding. If storage rejects, keep the current license live
+    // and do not deactivate the server behind the user's back.
     try{
-      await IDB.setMany([[IDB_KEY_LICENSE,null],[IDB_KEY_DEVICE_TOKEN,null]]);
+      const clears=ls?[[IDB_KEY_LICENSE,null]]:[[IDB_KEY_LICENSE,null],[IDB_KEY_DEVICE_TOKEN,null]];
+      await IDB.setMany(clears);
     }catch(e){
       return{success:false,error:'This browser could not save the deactivation. Allow site storage or free browser space, then try again.'};
     }
@@ -7647,7 +7655,10 @@
       const ok=await LicenseQueue.retryPost(WORKER_URL+'/api/license/deactivate',body);
       if(!ok){
         // Persist for next-startup drain so the LS instance doesn't leak active.
+        // The device token intentionally remains in IDB for that retry.
         LicenseQueue.push({action:'deactivate',license_key:ls.license_key,instance_id:ls.instance_id});
+      }else{
+        try{await IDB.set(IDB_KEY_DEVICE_TOKEN,null);}catch(e){/* server binding is already revoked */}
       }
     }
     return{success:true};
