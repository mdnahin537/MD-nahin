--- a/realmwright-v7.html
+++ b/realmwright-v7.html
@@ -7467,7 +7467,9 @@
     }
     console.warn('[license:queue] queued',this._logMeta(entry));
   },
-  // Retry one network POST with exponential backoff. Returns true on success.
+  // Retry one deactivation POST with exponential backoff. A 2xx status alone
+  // is not success: the Worker must explicitly confirm {deactivated:true}.
+  // Returns 'success', 'retry', or 'terminal' (a rejection retries cannot fix).
   async retryPost(url,bodyParams){
     const delays=[1000,2000,4000];
     for(let i=0;i<delays.length+1;i++){
@@ -7476,13 +7478,16 @@
         const headers={Accept:'application/json','Content-Type':'application/x-www-form-urlencoded'};
         if(tok)headers['X-Device-Token']=tok;
         const res=await fetch(url,{method:'POST',headers,credentials:'include',body:bodyParams});
-        if(res.ok)return true;
-        // 4xx is not retryable — the server has rejected the request, not a network blip.
-        if(res.status>=400&&res.status<500&&res.status!==408&&res.status!==429)return false;
+        let json=null;
+        try{json=await res.json();}catch(e){}
+        if(res.ok&&json?.deactivated===true)return'success';
+        // Explicit server rejection or non-retryable 4xx: another launch will not
+        // repair it. The Worker has already attempted to revoke its local binding.
+        if(json?.deactivated===false||(res.status>=400&&res.status<500&&res.status!==408&&res.status!==429))return'terminal';
       }catch(e){/* network — fall through to retry */}
       if(i<delays.length)await new Promise(r=>setTimeout(r,delays[i]));
     }
-    return false;
+    return'retry';
   },
   // Best-effort drain on startup. Logs failures with explicit tag so they
   // surface in DevTools without nagging the user.
@@ -7492,14 +7497,14 @@
     console.info('[license:queue] draining',arr.length,'entries');
     const survivors=[];
     for(const entry of arr){
-      let ok=false;
+      let result='terminal';
       try{
         if(entry.action==='deactivate'&&entry.license_key&&entry.instance_id){
           const body=new URLSearchParams({license_key:entry.license_key,instance_id:entry.instance_id});
-          ok=await this.retryPost(WORKER_URL+'/api/license/deactivate',body);
+          result=await this.retryPost(WORKER_URL+'/api/license/deactivate',body);
         }
-      }catch(e){console.error('[license:queue] drain error',e);}
-      if(!ok){
+      }catch(e){console.error('[license:queue] drain error',e);result='retry';}
+      if(result==='retry'){
         entry.attempts=(entry.attempts||0)+1;
         // Drop after 10 attempts — Worker's cleanup-orphans cron will catch it.
         if(entry.attempts<10)survivors.push(entry);
@@ -7652,13 +7657,15 @@
     document.dispatchEvent(new CustomEvent('license:changed'));
     if(ls){
       const body=new URLSearchParams(ls);
-      const ok=await LicenseQueue.retryPost(WORKER_URL+'/api/license/deactivate',body);
-      if(!ok){
+      const result=await LicenseQueue.retryPost(WORKER_URL+'/api/license/deactivate',body);
+      if(result==='retry'){
         // Persist for next-startup drain so the LS instance doesn't leak active.
         // The device token intentionally remains in IDB for that retry.
         LicenseQueue.push({action:'deactivate',license_key:ls.license_key,instance_id:ls.instance_id});
       }else{
-        try{await IDB.set(IDB_KEY_DEVICE_TOKEN,null);}catch(e){/* server binding is already revoked */}
+        // Success or a terminal rejection: no future retry can improve it, and the
+        // Worker has already attempted to remove its server-side device binding.
+        try{await IDB.set(IDB_KEY_DEVICE_TOKEN,null);}catch(e){/* token is non-authoritative */}
       }
     }
     return{success:true};
