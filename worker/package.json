{
  "name": "rw-license-worker",
  "version": "0.1.0",
  "private": true,
  "description": "RealmWright license/demo Worker. Proxies the Lemon Squeezy license API with a product-id paywall, verifies itch.io download keys, and runs the Turnstile-gated free-demo OpenRouter proxy. Device capping uses LS native activation_limit.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  }
}
